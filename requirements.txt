# 幕僚 Muliáo · 运行依赖
# 生成于实测通过的版本（Python 3.12.10，Windows 11）。
# 安装：pip install -r requirements.txt
#
# 说明：
#   · 下列为**直接依赖**，版本均为本机实测跑通工具调用链路时的版本。
#   · 标了 environment marker 的按平台条件安装。
#   · 括号内为会被自动带进来的主要**间接依赖**，无需手动列出，列此仅供核对：
#       starlette, pydantic, anyio, sniffio      (fastapi 传递)
#       click, h11, httptools                    (uvicorn 传递)
#       httpcore, certifi, idna                  (httpx 传递)

# Web 框架 + ASGI 服务器（后端 SSE 流式代理）
fastapi==0.116.1
uvicorn==0.52.1

# 上游模型 HTTP 客户端（/api/chat 流式代理、Jev 调用、缓存预热）
httpx==0.28.1

# 本机采集：进程/内存/磁盘/开机时长（collectors.system_info / processes）
psutil==7.2.2

# Windows 窗口枚举（collectors.windows 用 win32gui / win32process）
# 仅 Windows 需要；macOS / Linux 上该采集源自动降级、不影响其余功能。
pywin32==312 ; sys_platform == "win32"

# Windows 桌面控制（machine_control 用 pywinauto 的 uia backend 做点击/输入/组合键）
# 仅 Windows 需要；缺库或非 Win 时 machine_control 各函数降级返回 control_unavailable。
# comtypes 为 pywinauto 的传递依赖，pip 自动带入，无需单列。
pywinauto==0.6.9 ; sys_platform == "win32"
