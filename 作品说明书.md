# 幕僚 Muliáo · 作品说明书

> EvoTavern 进化酒馆黑客松（深圳站） · 赛道04「多Agent蜂群协作」
> 平台：Windows 本地优先桌面应用 · 黑金风格面板 · 仓库 private

---

## 一、一句话定位

**幕僚是一个装在你自己电脑上、默认什么都看不见、每一次动手前都要先过风险闸门的中文 AI 参谋。**

三行电梯陈述：

- **做了什么**：一个 Windows 本地桌面 Agent。它能读你的电脑（只读、需逐项授权）、能替你动手（点窗口、开应用、输入、按组合键）、能听懂你说话（离线唤醒 + 本地识别），还能召集一队分工明确的子 Agent（Ghost 蜂群）协作完成复杂任务。
- **为什么**：市面上的桌面 AI 要么"全知全能"地偷偷采集、要么动不动就替你执行危险操作。幕僚反其道而行——**默认全关、显式授权、每个动作执行前由独立的快判断引擎 Jev 打风险分，高风险弹确认，控制类在引擎掉线时一律拒绝（fail-closed）**。
- **怎么做**：FastAPI 后端 + SSE 流式前端，代理 OpenAI 兼容上游（阿里云千问），TypeSafe Jev 做结构化快判断，PyInstaller 打成单文件、Inno Setup 做成安装包。全部数据只存本机 `%APPDATA%\Muliao\`。

---

## 二、解决的真问题

让 AI 操作真实电脑，用户最怕三件事，幕僚逐条正面回答：

1. **"它是不是在偷偷看我？"**
   痛点：很多本地 Agent 默认就把通知、进程、浏览器历史、AI 对话日志一股脑喂给模型。
   幕僚的做法：**未授权的数据源对模型完全不可见**——不是事后拦截，而是根本不出现在工具列表里，模型连"可以调它"都不知道（`machine_tools.available_tool_specs()` 只返回已授权 scope 的 spec）。

2. **"它会不会闯祸？"**
   痛点：模型一旦决定调用 `删除`、`关机`、`alt+f4`、`发邮件`，用户往往来不及拦。
   幕僚的做法：**每个工具动作在真正执行前先过 `action_gate.evaluate()`**，由 Jev 打 1–10 的风险分，`allow / confirm / deny` 三态裁决；高风险弹前端确认，用户拍板后才执行。

3. **"我的数据会离开这台机器吗？"**
   痛点：前端直连上游、key 散落各处、数据上云。
   幕僚的做法：**前端不直连上游、key 不入库**；除"对话模型"和"Jev 判断"两处必需外联外，全部数据只存本机。仓库不含任何真实 API key（有专门的测试守住这条线）。

---

## 三、核心能力

### 1. Jev 决策门控（每一次动手都先过闸）

`action_gate.py` 是执行前的风险闸门，纯逻辑、不碰 IO。`/api/chat` 里每一个 tool_call 在交给 `machine_tools.execute_tool` 执行**之前**，先由 `evaluate()` 裁决：

- **allow** —— 直接执行（只读采集、低风险动作）。
- **confirm** —— 先发 `tool_confirm` 事件、挂起，等用户在前端点"允许执行"再决定。
- **deny** —— 拒绝执行，回灌拒绝占位结果，绝不真正调用工具。

**它怎么工作**：Jev 对"工具名 + 参数 + 用户当前意图"这套上下文打四个结构化判断——`risk_score`（1–10 分）、`reversible`（可否撤销）、`needs_confirm`（是否需人确认）、`matches_intent`（是否忠于用户意图）。阈值是常量 `CONFIRM_THRESHOLD = 6.0`：风险分 ≥6 即升级确认。

两条安全底线写死在代码里：

- **控制类 fail-closed**：Jev 不可用（超时/断网/额度）时，**控制类动作一律 `deny`**，绝不因为判断引擎掉线就放行危险操作；只读采集则降级放行（`allow`），不打扰用户。
- **本地危险词兜底（defense in depth）**：即便 Jev 判低风险，只要动作/参数命中本地危险词表（`rm -rf`、`format`、`shutdown`、`alt+f4`、`删除`、`发邮件`、`付款`、`密码`、`sudo` 等）且是控制类，**至少 confirm，不可被 Jev 放行绕过**。词表匹配带词边界，避免 `payload` 误命中 `pay`。

确认挂起用 `asyncio.Future` 在"流生成器"和"确认 POST 端点 `/api/chat/confirm`"之间搭桥；超时（默认 120 秒）或客户端断流一律按"拒绝"处理（fail-safe）。

### 2. 权限感知采集（默认关，逐项授权）

`permissions.py` 是同意闸门。授权记录原子写入 `%APPDATA%\Muliao\consent.json`，含逐项开关和授权时间戳。

- **7 个只读数据 scope**：`notifications`（系统通知）、`processes`（进程列表）、`windows`（窗口标题）、`browser`（浏览器历史）、`ai_logs`（Claude Code / Codex 对话日志）、`files`（桌面/文档/下载的文件名，**不读内容**）、`system`（主机名、系统版本、磁盘余量）。
- **2 个能力 scope**：`voice_control`、`computer_control`——**独立于数据采集，永不随"全部数据"授权打开**。代码注释写得很清楚："'全选'只代表七类只读数据；已有能力授权保持不变，不可偷开或误关麦克风。"
- **默认全关**：`_DEFAULT` 里所有 scope 初始为 `False`，未同意总条款或未授权该 scope，采集一律返回空，绝不偷采。用户可一键撤销（`revoke_all`）或彻底焚毁 consent 文件（`purge_all`）。

**它怎么工作**：采集前 `permissions.is_granted(scope)` 是硬闸门；授权是"用户显式同意"（隐私合规层面），与各采集源自报的 `available`（OS 能力层面）分开，两者都满足才真正产出数据。文件工具只列文件名/大小/时间，不读内容；错误文本回灌上游前会脱敏（抹掉本机用户目录、用户名）。

### 3. 电脑控制（pywinauto 桌面自动化）

`machine_control.py` 是纯执行能力层，把 Windows 桌面控制封装成 7 个结构化、可离线测试的函数：

| 函数 | 作用 |
|---|---|
| `list_windows` | 列出顶层窗口（上限 200 防呆，避免把整机灌进上下文） |
| `focus_window` | 聚焦/激活指定窗口 |
| `close_window` | 关闭窗口 |
| `open_application` | 启动应用 |
| `click_element` | 点击窗口内控件（pywinauto uia backend） |
| `type_text` | 向控件输入文字 |
| `press_keys` | 发送组合键 |

**它怎么工作**：这 7 个函数都绑定到 `computer_control` 能力 scope（默认关），每次执行前都过"权限闸门 + Jev 门控"。`machine_control.py` 本身**不含任何权限判断**——它只负责"能不能做、怎么做"，"该不该做"永远在调用方。防御式设计：pywinauto 未装或非 Windows 时，每个函数返回结构化失败 `{"ok": False, "error": "control_unavailable", ...}`，绝不抛异常到调用方；底层 GUI 自动化通过模块级 seam `_backend()` 注入，测试可 monkeypatch 假 backend，在没有真实桌面的 CI 上离线跑通。

### 4. 言出法随语音子系统（离线优先）

`voice/` 是一个**完全独立**的子系统，通过 `app.include_router(voice.api.router, prefix="/api/voice")` 接入，刻意不触碰聊天 `_sessions`、`_cache_stats`、`SYSTEM_PROMPT` 或 `machine_tools`。

链路：**唤醒 → 本地 ASR → 意图 → 门控 → 动作**，离线优先。固定唤醒词"幕僚幕僚"；未唤醒时不上传、不暴露环境语音原文；本地 ASR 用 sherpa-onnx 的 SenseVoice，离线 TTS 用 VITS。

5 个端点（`voice/api.py`）：`GET /status`、`POST /start`、`POST /stop`、`POST /command/test`、`GET /events`（SSE）。事件统一 `voice.*` 命名空间：`voice.state / partial / final / decision / confirmation / action / tts / metric / error`——未知事件前端忽略，禁止裸 `done/error/delta`。

**它怎么工作**：语音权限沿用主线的 `voice_control` 能力 scope（默认关）。未授权时 `/api/voice/start` 直接返回 403 拒绝，采音/Jev/动作调用数均为 0。**麦克风用完即释放**：进程关闭时 lifespan 只调用 `shutdown_voice_service()` → `service.stop()`，释放麦克风、HTTP client 和播放器。开发验收默认 dry-run，只有显式 `--act` 才真正打开记事本。

### 5. Ghost 蜂群编排（多 Agent 协作）

`swarm.py` 是供应商无关的编排核心：确定性规划、任务契约、asyncio 编排，不直接依赖模型/HTTP/权限/持久化——调用方注入 `bee_runner`（执行一只蜂）和 `jev_checker`（批量小判断）。

- **6 个角色**（`ROLE_POOL`）：`compiler`（把目标编译成约束与验收标准）、`investigator`（收集并区分观察/推断/待核验）、`extractor`（提取结构化事实）、`builder`（在契约内产出实现）、`verifier`（独立核验、指出冲突缺口）、`integrator`（整合各蜂结果给最终答复）。
- **5 套固定编排配方**（`RECIPES`）：`single / research / diagnose / build / sensitive`，按任务类型走不同的分阶段角色流水线。

**它怎么工作**：由 Jev 规划——`SWARM_PLAN_QUESTIONS` 让 Jev 判断任务是否 `swarm_worthy`（多 Agent 是否真有增益）、选哪个 `recipe_id`、`risk_score`、是否需要澄清、是否证据密集、能否并行。Jev 不可用时回落到 `deterministic_fallback` 确定性规划。**可恢复持久化**：`swarm_store.py` 用 SQLite（schema v2）分离全局序列与 run-local 事件序列，记录可恢复的 run 元数据，原子持久化每个 WorkCapsule 及其接收方 ACK；`swarm_persistence.SwarmLedger` 是不含 HTTP/路径知识的全局账本，运行可从中断处恢复。

### 6. 本地优先 / 隐私

- 数据只在本机 `%APPDATA%\Muliao\`，**不上传**；除对话模型与 Jev 判断外无任何外联。
- **前端不直连上游**：server 代理上游、SSE 流式转发（含 `reasoning_content` 思考流），key 只存在于服务端进程内存/环境变量/仓库外 config，前端拿不到。
- **key 不入库**：仓库不保存任何 API 密钥。开发用环境变量（`MULIAO_LLM_KEY` 等）或仓库外 `%APPDATA%\Muliao\config.json`，环境变量优先。

---

## 四、技术架构

- **后端**：FastAPI 0.116.1 + uvicorn 0.52.1，SSE 流式响应（对话 delta、思考流、工具门控事件、语音事件全走 SSE）。
- **上游**：OpenAI 兼容协议。默认接阿里云千问网关（`model_catalog.py`：基址 `https://maas.qianwenaiapi.com/compatible-mode/v1`，默认主模型 `qwen3.8-max`，另有 `qwen3.7-plus` / `qwen3.8-flash`）。`/api/models` 探测这把 key 真正被授权的模型（source=`probe`），探测失败回落到按官方文档整理的目录（source=`documented`，未验证）；成功/失败分别缓存 5 分钟/20 秒，key 修好后无需重启即自动出现。
- **快判断引擎**：TypeSafe Jev（System One 模型），做回合前体检、回合后复核、动作门控、蜂群规划四类结构化判断。
- **桌面控制**：pywinauto 0.6.9（uia backend）、pywin32 312、psutil 7.2.2。
- **语音**：sherpa-onnx 1.13.8（本地 SenseVoice ASR / 离线 VITS TTS）。
- **打包**：PyInstaller 打成单文件（`muliao.spec`：`EXE(... name='Muliáo', console=False)`，windowed 无控制台），Inno Setup 做成安装包（`installer.iss`，`幕僚Muliao-Setup.exe`，版本 1.0.0，一键装到 `D:\Program Files\幕僚Muliáo`，不需管理员权限）。

**prompt 缓存稳态**：这是命中率的关键工程。上游按"前缀逐字节匹配"命中缓存（TTL>65s，最小 ~128 tokens）。实测规律——system 提示词越长越稳命中率越高：74 tokens 时多轮才 55%，~2000 tokens 时第 2 轮起稳定 95%+。因此：

- **`SYSTEM_PROMPT` 冻结**：这段长前缀必须逐字节永不变，不含时间戳/随机数/动态内容，由后端统一注入、前端不拼。它的 SHA-256 被测试钉死为 `50e07b3b31dc5ca87420135484c4002cbce6878ece982defb026ce36e12e7ea7`（见下）。
- 工具结果裁剪到默认 1500 字符（`machine_tools._trim`）——工具返回是全新内容必然 miss，体积直接决定当轮命中率下限（实测未裁剪的进程列表 2600+ 字符会把整轮拉到 60%）。
- 右栏"📊 缓存命中率"面板展示稳态命中率，达标线 90%。

---

## 五、工程严谨性

- **测试规模**：`pytest tests/` 共 **780 个用例收集并全绿**（780 passed，另有 105 subtests passed，28 秒跑完）。其中语音线约 **550** 个、主线约 **230** 个。测试覆盖门控、权限、机器控制、蜂群（规划/持久化/端到端/UI/handoff）、语音（API/ASR/TTS/编排/安全/并发/web 验证器）等。
- **SYSTEM_PROMPT 哈希冻结**：`tests/test_repository_guards.py` 用 AST 静态解析 server.py 里的 `SYSTEM_PROMPT`（含字符串拼接与 `* 60` 重复），算 SHA-256 并断言等于冻结值。**集成语音、Ghost、阿里云模型后，这段提示词未变一字节**（本次写作前实测验证：哈希与冻结值一致）。
- **端到端真机验证**：`deploy.py` 的冒烟测试用隔离临时 APPDATA 验证——权限闸门未授权时无泄漏、授权后 7 个源真能采到数据、模型真会调工具、Jev 判断可用、缓存命中率 ≥90%、静态资源齐全；不动用户真实授权状态。语音另有真 Jev dry-run 与真实麦克风验收（见 §八）。
- **fail-closed 安全设计**：控制类动作在 Jev 掉线时一律 deny；确认超时/断流一律按拒绝；本地危险词命中不可被 Jev 放行绕过；权限保存失败必须抛出，API 不虚报成功。
- **CSRF / 同源防护**：`TrustedHostMiddleware` 只允许 `127.0.0.1 / localhost / [::1]`；自定义中间件 `protect_local_api` 对所有 `/api/*` 写方法（POST/PUT/PATCH/DELETE）校验 Host 为固定回环（不依赖请求 Host 动态生成信任列表，避免 DNS rebinding）、校验 Origin 同源、拒绝 `sec-fetch-site: cross-site`、强制 `application/json`。`/api/notify/journal` 另做字段强制 str + 长度封顶，防伪造通知钓鱼/撑爆 journal。
- **无硬编码密钥**：`tests/test_repository_guards.py` 扫描全仓库（`.py/.js/.html/.css/.md/.json/.bat/.iss/.spec`），断言不含 `sk-…`、`apikey_…`、`ghp_…`、`github_pat_…` 形态的真实凭据。

---

## 六、与赛道04「多Agent蜂群协作」的呼应

Ghost 蜂群是本作品对赛道命题的正面作答，而且不是"为多 Agent 而多 Agent"：

- **真分工**：6 个角色（compiler / investigator / extractor / builder / verifier / integrator）职责互不重叠，`verifier` 专门独立核验、指出冲突与不可证实之处——把"自己查自己"变成"另一只蜂来查"。
- **真编排**：5 套配方按任务走不同的分阶段流水线，同阶段内可多蜂并行（`parallelizable` 由 Jev 判断），阶段间用 WorkCapsule 交接。
- **真门控**：蜂群的每一步规划由 Jev 打分，高风险/敏感配方（`sensitive`）走确认；`evaluate_user_gate` + `risk_gate` 把用户意图和风险等级纳入规划。
- **真持久化、可恢复**：SQLite schema v2 记录 run 元数据与事件序列，原子持久化每个 capsule 及其 ACK，运行可从中断处恢复——蜂群不是"跑完就没"的内存玩具。
- **克制**：`swarm_worthy` 判断为否时，直接走单 Agent，不硬凑蜂群。

蜂群与语音、机器控制共享同一套 Jev 门控与权限模型——**多个 Agent 协作时，安全边界不因为"蜂多了"而松动。**

---

## 七、现场演示脚本（4 分钟版）

> 建议提前在隔离数据目录里授权好 7 个数据 scope，主对话 key 用一把有效 key（见 §八）。

1. **（0:00–0:30）开场 + 隐私姿态**
   打开面板，指右栏"本地优先"。说："这是一个装在本机的中文参谋。它默认什么都看不见——未授权的数据源根本不出现在模型的工具列表里，不是事后拦截，是模型连调都不知道能调。"

2. **（0:30–1:20）权限感知采集**
   进权限页逐项勾选 7 个只读源，回对话问："我现在前台开着什么程序？" / "什么在吃内存？"。说："它自己决定要不要读电脑来回答。全部数据只存本机，除对话模型和 Jev 判断外零外联。"

3. **（1:20–2:20）Jev 门控 + 高风险确认（核心）**
   问一个只读问题，指右栏"⚖ 本回合体检"展示 Jev 的逐项判断（意图/危险度/外泄/需澄清 + 概率分布）。然后请它做一个控制类高风险动作（如关闭某窗口/删除类）。说："看，每个动作执行前先过 Jev 打分——这是 `tool_gate` 事件，风险分 ≥6 就弹确认，用户拍板才执行。如果 Jev 掉线，控制类动作直接拒绝，绝不放行。"展示前端确认弹窗，点"允许"。

4. **（2:20–3:10）语音"言出法随"**
   开 `voice_control` 能力 scope，说"幕僚幕僚，打开记事本"。说："离线唤醒 + 本地识别，唤醒才上传，麦克风用完即释放。"（现场可用 dry-run 展示事件流，真机执行作为备用。）

5. **（3:10–4:00）Ghost 蜂群**
   提一个需要多步的任务，展示蜂群按配方分工、各角色产出、verifier 独立核验、integrator 汇总。说："6 个角色分工、Jev 规划、SQLite 可恢复持久化。多个 Agent 协作时，安全边界不松动——每一步仍过同一套门控。"指右栏"📊 缓存命中率"收尾："长稳定前缀让稳态命中率 ≥90%，这是成本和延迟的关键。"

---

## 八、已知边界与诚实声明

不美化，如实说明：

1. **SYSTEM_PROMPT 哈希冻结**：`50e07b3b31dc5ca87420135484c4002cbce6878ece982defb026ce36e12e7ea7`。集成语音、Ghost、阿里云模型后**未变一字节**（本次写作前用 AST 解析 + SHA-256 实测确认一致）。这是 prompt 缓存命中率的前提，也是回归红线。

2. **阿里云对话 key 目前被服务端拒（`invalid_api_key`）**：接入代码已就绪、测试全绿、`/api/models` 探测与回落逻辑完整，但**需要一把有效的 key 才能发出真实对话回复**。在拿到有效 key 之前，主对话链路无法产生真实回复——**这一点如实写明，不假装对话已联通**。现场演示需自备有效 key。（注：`使用说明.md` 第六节关于"key 硬编码在 server.py 顶部"的描述是早期版本的过时说明；当前代码从环境变量/仓库外 config 读取，且有 `test_repository_guards.py` 守住"无内嵌凭据"。）

3. **桌面 GOAL 仍 dry-run**：语音/桌面的目标执行默认 dry-run，只有显式 `--act` 才真正启动应用（如记事本）。真麦克风、真记事本作为单独硬件验收，不在自动部署中执行。

4. **云 ASR / 离线 VITS 默认关**：语音离线优先；云端 ASR 与离线 VITS TTS 属可选路径，默认关闭。

5. **通知抓取只在 Windows 实测过**（只读 `wpndatabase.db`，零权限零依赖）。macOS / Linux / Android 的代码写好了但**没在真机验证过**，权限页会如实标注"本机不可用"，不假装支持。微信走 WeChatBridge 归档文件（Obsidian 笔记 / 导出 ZIP），样例验证过，未在真实归档上跑过。

6. **上游 `max_tokens` 不生效**：对话模型回复长度压不住；缓存命中率靠长稳定前缀摊薄，单轮遇超长回复会暂时掉到 86–88%，稳态仍 ≥90%。

7. **仓库 private，不含任何真实 API key。**
