# 幕僚 Muliáo · 使用说明

本地优先的 AI 参谋。仿 ZCode 的纯黑界面，Jev 负责快判断，主模型负责想和写。

---

## 一、安装（装到 D 盘）

双击桌面的 **`幕僚 Muliáo · 安装到D盘`**，它会自动装到 `D:\Program Files\幕僚Muliáo\`，不用你选目录。

或者手动跑安装包：`D:\Program Files\幕僚Muliáo-Setup.exe`（也在 `muliao\dist\` 里有一份）。

装完后桌面会有 **`幕僚 Muliáo`** 快捷方式，双击即用。开始菜单里也有，带卸载入口。

> 不需要管理员权限。卸载走「设置 → 应用」或开始菜单里的 Uninstall 快捷方式。
> 首次运行可能弹 SmartScreen 蓝色提示（因为没做代码签名），点「更多信息 → 仍要运行」即可。

---

## 二、第一次打开：权限确认

幕僚是本地优先的参谋，**默认什么都采不到**。第一次打开会弹权限页，逐项列出它能访问什么：

| 数据源 | 具体读到什么 | 敏感度 |
|---|---|---|
| 系统通知 | 全系统 toast（来源应用、标题、正文）+ 微信归档 | 敏感 |
| 进程列表 | 所有运行中进程：名称、内存、完整命令行 | 敏感 |
| 窗口标题 | 所有顶层窗口标题、所属进程、当前前台窗口 | 敏感 |
| 浏览器历史 | Chrome / Edge 访问过的网址与标题 | 敏感 |
| AI 工具对话日志 | Claude Code / Codex 的历史对话 | 敏感 |
| 文件清单 | 桌面 / 文档 / 下载的文件名、大小、时间（**不读内容**） | 敏感 |
| 系统信息 | 主机名、用户、系统版本、磁盘余量、开机时长 | 一般 |

规则：

- **没勾的就采不到**。未授权的数据源不会出现在模型的工具列表里——模型连"可以调它"都不知道，不是靠事后拦截。
- 全部数据只存在本机 `%APPDATA%\Muliao\`，**不上传**。除对话模型与 Jev 判断外无任何外联。
- 幕僚**只读**：不改你的文件、不注入进程、不改系统设置。
- 随时可改：左栏底部「本地优先」那一行点进去就能重新勾选，或一键撤销。

不勾也能用——只是幕僚看不到你的电脑，退化成普通对话助手。

---

## 三、界面

```
┌────────────┬──────────────────────────┬──────────┐
│ 左栏        │ 中栏                      │ 右栏      │
│            │                          │          │
│ 新建会话    │ 顶栏：会话名 · 引擎状态    │ ⚖ 体检    │
│ 搜索会话    │                          │ 🔔 通知   │
│ 导航        │ 对话区（闪光金图标）       │ 📊 缓存   │
│ 会话列表    │                          │          │
│ 引擎状态    │ 快判断条                  │          │
│ 本地优先    │ 悬浮输入框 · 状态栏        │          │
└────────────┴──────────────────────────┴──────────┘
```

- **左栏**：新建会话（`Ctrl+N`）、搜索（`Ctrl+K`）、三个导航入口、会话列表、引擎状态灯、底部权限入口
- **中栏**：对话。发消息时输入框上方会弹出 Jev 的快判断 chip（意图/危险度/外泄/需澄清）
- **右栏三个标签**：
  - **⚖ 本回合体检**：Jev 逐项判断的明细（含概率分布条形图、门控结果）+ 答完后的复核结论（切题/安全/克制）
  - **🔔 系统通知**：本机通知，按「需要处理 / 可留意 / 其余」分组，Jev 给每条分类并打「需处理」概率。每条可一键「问幕僚」
  - **📊 缓存命中率**：prompt 缓存的稳态命中率，达标线 90%

助手回复上方有个**可折叠的「思考中…」**，展开能看模型的推理过程（流式实时刷新）。

侧栏和右栏都能收起（各自的箭头/×按钮），收起后左上/右上会出现小按钮展开。

---

## 四、它能干什么

授权之后，幕僚能**自己决定**要不要读你的电脑来回答你。比如：

- 「我现在前台开着什么程序？」→ 调窗口工具，告诉你真实的前台窗口
- 「什么在吃内存？」→ 调进程工具，按内存降序给列表
- 「有什么消息吗？」→ 调通知工具，读最近的系统通知
- 「我刚下载的那个文件叫什么？」→ 调文件工具，列下载目录
- 「我刚才看的那个网页」→ 调浏览器历史工具
- 「之前让 AI 做过什么方案」→ 检索 Claude Code / Codex 的历史对话

不需要你指定用哪个工具，它自己判断。没授权的部分它会直说"未获授权"，不会编。

另外每条消息都会经过 **Jev 快判断**（意图分类、危险等级、外泄风险、是否需澄清），回答完还会**复核**（切题吗、安全吗、有没有编造）。危险操作会先跟你确认。

---

## 五、开发 / 重新构建

```bash
cd "D:\2026暑假一切\进化酒馆\muliao"

# 开发态跑（改完即生效，不用构建）
python server.py                 # → http://127.0.0.1:8930

# 一键构建 + 卸载旧版 + 装 D 盘 + 建快捷方式 + 冒烟测试
python deploy.py

# 只想重装，不重新构建
python deploy.py --no-build

# 只想卸载
python deploy.py --uninstall
```

`deploy.py` 的冒烟测试会验证：权限闸门未授权时无泄漏、授权后 7 个源真能采到数据、模型真会调工具、Jev 判断可用、缓存命中率 ≥90%、静态资源齐全。**用隔离的临时 APPDATA 跑，不会动你的真实授权状态。**

依赖：`requirements.txt`（fastapi / uvicorn / httpx / psutil / pywin32）。打包链：PyInstaller + Inno Setup。

> ⚠️ 别用 `cmd //c build.bat`（在 Git Bash 里会报"不是内部或外部命令"），用 `python deploy.py`。

---

## 六、已知限制（如实说明）

- **通知抓取只在 Windows 实测过**（只读 `wpndatabase.db`，零权限零依赖）。macOS / Linux / Android 的代码写好了但**没在真机验证过**，权限页会如实标注"本机不可用"，不会假装支持。
- **微信**走 WeChatBridge 的归档文件（Obsidian 笔记 / 导出 ZIP），不是直接读微信——微信 PC 版的聊天提示是自绘窗口，不走系统通知。这条路径用样例验证过，没在真实归档上跑过。
- 对话模型的 `max_tokens` 参数在上游**不生效**，所以回复长度压不住；缓存命中率靠长稳定前缀摊薄，单轮遇到超长回复会暂时掉到 86-88%，稳态仍 ≥90%。
- **API key 不进仓库、不进 exe**：密钥只从环境变量或仓库外的 `%APPDATA%\Muliao\config.json` 读取，`server.py` 顶部只有 `_setting()` 取值调用，没有任何硬编码凭据；`tests/test_repository_guards.py` 会扫描全仓库（`.py/.js/.html/.css/.md/.json/.bat/.iss/.spec`）断言不含真实密钥形态，一旦有人误提交就红灯。安装包因此可以分发——拿到 exe 的人仍需自己填 key 才能用。
- **上游地址按套餐走**：阿里云百炼的套餐专属 key（`sk-sp-` 前缀）必须配对应的套餐 Base URL，用通用 `dashscope`/`maas` 域名会被判 `invalid_api_key`。
- 工具调用轮数默认不限（`MULIAO_MAX_TOOL_ROUNDS=0`），模型可以一路调到问题真正解决；只有硬安全阀 `MULIAO_MAX_TOOL_ROUNDS_CAP`（默认 64）会拦住无限自我调用，撞到时会撤掉工具强制收尾作答，不会静默无回复。
- 缓存命中率面板的「本轮」数字对工具调用轮会偏低——工具返回的内容是全新的，天然无法命中缓存。看「稳态」那个数。

---

## 七、卸载

「设置 → 应用 → 幕僚 Muliáo → 卸载」，或开始菜单里的 Uninstall 快捷方式。

卸载**不会**删你的数据。要彻底清掉，再删这个目录：`%APPDATA%\Muliao\`（里面是授权记录和日志）。
